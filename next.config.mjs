/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      unoptimized: true, 
      domains: ['https://www.rameentourandtravels.com'],  
    },
  };
  
  export default nextConfig;
  